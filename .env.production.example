# Production Environment Variables for Vercel
# Copy these to Vercel Dashboard → Project Settings → Environment Variables

# Razorpay LIVE Keys (DO NOT use test keys in production!)
# Get from: https://dashboard.razorpay.com/app/keys (Switch to LIVE MODE)
NEXT_PUBLIC_RAZORPAY_KEY_ID=rzp_live_xxxxxxxxxxxxx
RAZORPAY_KEY_SECRET=your_live_secret_key_here

# Razorpay Webhook Secret
# Get from: https://dashboard.razorpay.com/app/webhooks
# Create webhook with URL: https://yourdomain.vercel.app/api/razorpay/webhook
# Select events: payment.authorized, payment.captured, payment.failed
RAZORPAY_WEBHOOK_SECRET=your_webhook_secret_here

# Production Domain (change to your actual domain)
# Examples:
# - Vercel subdomain: https://personalize-online.vercel.app
# - Custom domain: https://yourdomain.com
NEXT_PUBLIC_BASE_URL=https://personalize-online.vercel.app

# Node Environment (Vercel sets this automatically, but good to have)
NODE_ENV=production

# ============================================
# IMPORTANT NOTES:
# ============================================
# 1. NEVER commit this file with real values to Git
# 2. Use LIVE keys (rzp_live_*) NOT test keys (rzp_test_*)
# 3. Razorpay KYC must be completed before getting live keys
# 4. Update NEXT_PUBLIC_BASE_URL to your actual domain
# 5. Set up webhook AFTER deploying to get the correct URL
# 6. All variables marked NEXT_PUBLIC_* are exposed to frontend (safe)
# 7. Keep RAZORPAY_KEY_SECRET private (server-side only)
